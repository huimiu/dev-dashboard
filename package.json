{
  "name": "dev-dashboard",
  "version": "0.1.0",
  "engines": {
    "node": "14 || 16 || 18"
  },
  "license": "MIT",
  "devDependencies": {
    "@microsoft/teamsfx-cli": "1.*"
  },
  "scripts": {    
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
